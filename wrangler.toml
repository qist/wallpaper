# Cloudflare Pages 本地调试配置 (仅供本地预览 Functions 用)
# 本地预览: npx wrangler pages dev .
# 线上部署无需 wrangler: CF Pages 构建命令留空 + 输出目录 / 即可自动识别
name = "wallpaper"
pages_build_output_dir = "."
compatibility_date = "2026-08-15"
